var myDialog = app.dialogs.add( {name: "Rotate Individuals", canCancel: true} );//Add a dialog column.with( myDialog ) {	with(dialogColumns.add()){		staticTexts.add({staticLabel: "Rotate by:"});	}	with(dialogColumns.add()){		var myRotationField = angleEditboxes.add({editValue: 0});	}}if(myDialog.show() == true){		// Get user input	var myRotation = myRotationField.editValue;		// Get selected items	var mySelection = app.activeDocument.selection;		// Define rotation	var myRotateMatrix = app.transformationMatrices.add({counterclockwiseRotationAngle: myRotation});		// Rotate each item individually	for(i=0; i<mySelection.length; i++) {		mySelection[i].transform(CoordinateSpaces.pasteboardCoordinates, AnchorPoint.centerAnchor, myRotateMatrix);	}}else{	myDialog.destroy()}